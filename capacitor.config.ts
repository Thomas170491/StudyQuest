import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.thomaspapas.nomades',
  appName: 'studyquest',
  webDir: 'dist/study-quest'
};

export default config;
