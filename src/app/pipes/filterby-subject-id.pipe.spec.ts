import { FilterbySubjectIdPipe } from './filterby-subject-id.pipe';

describe('FilterbySubjectIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbySubjectIdPipe();
    expect(pipe).toBeTruthy();
  });
});
