import { FilterByChapterPipePipe } from './filter-by-chapter-pipe.pipe';

describe('FilterByChapterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByChapterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
